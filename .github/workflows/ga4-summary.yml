name: Monthly GA4 Report Summary

on:
  schedule:
    - cron: '0 0 1 * *' 
  workflow_dispatch: 

permissions:
  id-token: write
  contents: read
  issues: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install google-analytics-data google-genai

    - id: 'auth'
      uses: 'google-github-actions/auth@v2'
      with:
        workload_identity_provider: '${{ secrets.WIF_PROVIDER }}'
        service_account: '${{ secrets.GCP_SA_EMAIL }}'

    - name: Fetch GA4 Report and Analyze with Gemini
      env:
        GA4_PROPERTY_ID: ${{ secrets.GA4_PROPERTY_ID }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: python get_ga4_data.py

    - name: Create GitHub Issue
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        # 記録用としてGitHubのIssueにも投稿しておきます
        gh issue create --title "【月次】GA4自動分析レポート＆施策提案" --body-file issue_body.md

    - name: Send Full Report to Slack
      run: |
        # 作成されたレポート全文を読み込み、安全な形式に変換してSlackに直接長文送信します
        REPORT_TEXT=$(cat issue_body.md)
        jq -n --arg text "📊 *【月次】GA4自動分析レポート*\n\n$REPORT_TEXT" '{text: $text}' | curl -X POST -H 'Content-type: application/json' -d @- ${{ secrets.SLACK_WEBHOOK_URL }}
